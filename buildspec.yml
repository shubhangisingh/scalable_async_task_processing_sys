version: 0.2

phases:
  install:
    runtime-versions:
      ruby: 3.3
    commands:
      - bundle install

  build:
    commands:
      - bundle exec rails db:create RAILS_ENV=test
      - bundle exec rails db:migrate RAILS_ENV=test
      - bundle exec rspec

artifacts:
  files:
    - '**/*'